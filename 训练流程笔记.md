# PCVRHyFormer 训练流程笔记

> 整理日期：2026-05-12  
> 入口文件：`run.sh` → `train.py`  
> 数据来源：`get_data_sample.py`（从 HuggingFace 拉取 Parquet 样本）

---

## 0. 一句话概述

本项目以 **PCVR（Post-Click Conversion Rate）预测**为任务目标，基于 HyFormer 论文实现了 `PCVRHyFormer`：将多条异构行为序列（seq_a/b/c/d）与非序列（NS）特征联合建模，通过「Query Decoding + Query Boosting」逐层双向交互，最终输出 CTR/PCVR 二分类 logit。

---

## 1. 启动链路：run.sh → train.py

```
bash run.sh [--batch_size 512 --lr 3e-4 ...]
    └── python3 train.py \
            --ns_tokenizer_type rankmixer \
            --user_ns_tokens 4 \
            --item_ns_tokens 2 \
            --num_queries 2 \
            --ns_groups_json "" \
            --emb_skip_threshold 1000000 \
            --num_workers 8 \
            "$@"
```

`run.sh` 做了三件事：
1. 将脚本所在目录加入 `PYTHONPATH`，使同级模块无需安装即可 import。
2. 固定当前激活配置（RankMixer NS Tokenizer，无需 `ns_groups.json`）。
3. 透传 `"$@"` 给 `train.py`，支持在调用侧覆盖任意超参。

### 两种 NS Tokenizer 配置对比

| 配置 | 参数 | num_ns | T（d_model=64） | 约束 |
|------|------|--------|----------------|------|
| **RankMixer**（当前激活） | `user_ns_tokens=4, item_ns_tokens=2, num_queries=2` | 4+1+2+1=8 | 2×4+8=**16** | 64%16=0 ✓ |
| GroupNSTokenizer（备选） | `ns_groups_json=ns_groups.json, num_queries=1` | 7+1+4=12 | 1×4+12=**16** | 64%16=0 ✓ |

> **T = num_queries × num_seq_domains + num_ns**，需满足 `d_model % T == 0`（RankMixerBlock token mixing 约束）。

---

## 2. 数据样本来源（get_data_sample.py）

```python
import pandas as pd
df = pd.read_parquet("hf://datasets/TAAC2026/data_sample_1000/demo_1000.parquet")
```

- 数据托管在 HuggingFace：`TAAC2026/data_sample_1000`，共 1000 条演示样本。
- 完整训练数据为同一格式的多文件 Parquet，放置于 `--data_dir` 指定目录。
- 列格式：`user_int_feats_{fid}`、`item_int_feats_{fid}`、`user_dense_feats_{fid}`、`seq_a_{fid}` …… + `label_type`、`timestamp`、`user_id`。

---

## 3. 数据管道（dataset.py）

### 3.1 特征 Schema（schema.json）

`schema.json` 是特征元数据的唯一来源，格式：

```json
{
  "user_int": [[fid, vocab_size, dim], ...],
  "item_int": [[fid, vocab_size, dim], ...],
  "user_dense": [[fid, dim], ...],
  "seq": {
    "seq_a": {"prefix": "seq_a", "ts_fid": <int>, "features": [[fid, vocab_size], ...]},
    ...
  }
}
```

`FeatureSchema` 对象记录每个特征的 `(fid, offset, length)`，供模型按偏移切片。

### 3.2 数据集切分（Row Group 粒度）

```
所有 Parquet Row Groups
├── [0, n_train_rgs)        → 训练集（头部，可用 train_ratio 进一步缩减）
└── [n_train_rgs, total_rgs) → 验证集（尾部 valid_ratio 比例，默认 10%）
```

两者以 `n_train_rgs` 为界，严格不重叠，且与数据写入顺序一致，保证可复现。

### 3.3 PCVRParquetDataset（IterableDataset）

流式读取 Parquet，每次返回一个 batch dict，关键字段：

| 字段 | 形状 | 含义 |
|------|------|------|
| `user_int_feats` | `(B, user_int_dim)` | 用户整型特征（展平） |
| `item_int_feats` | `(B, item_int_dim)` | 物品整型特征（展平） |
| `user_dense_feats` | `(B, user_dense_dim[+5])` | 用户连续特征（含时间特征） |
| `item_dense_feats` | `(B, 0)` | 物品连续特征（当前为空） |
| `time_feats` | `(B, 2)` | 离散时间 id：`[hour(1..24), dow(1..7)]` |
| `{domain}` | `(B, n_side_feats, max_len)` | 序列 side-info id（各域独立） |
| `{domain}_len` | `(B,)` | 序列有效长度 |
| `{domain}_time_bucket` | `(B, max_len)` | 行为时间桶 id（64 条分桶边界） |
| `label` | `(B,)` | 二分类标签（`label_type == 2` 为正例） |

**时间特征扩展**（`use_time_features=True`）：从 `timestamp` 列派生 5 个浮点特征（`hour_sin/cos`、`dow_sin/cos`、`is_weekend`）追加到 `user_dense_feats` 末尾，`user_dense_dim` 自动扩大 5。

**Shuffle 策略**：IterableDataset 不支持全局 shuffle，采用「缓冲区 shuffle」：缓积 `buffer_batches` 个 batch 后行级随机打乱再 yield。

**DataLoader 配置**：
- 训练：`num_workers=8, pin_memory=True, persistent_workers=True, prefetch_factor=2`
- 验证：`num_workers=0`（串行，无需预取）

---

## 4. 模型构建（model.py）

### 4.1 整体结构

```
原始特征
  ↓
[NS Tokenizer]              整型/Dense 特征 → (B, num_ns, D) NS tokens
  ↓
[Seq Embedding]             各序列域 → (B, L_i, D) seq tokens（含时间桶 Embedding）
  ↓
[MultiSeqQueryGenerator]    NS tokens + seq MeanPool → (B, Nq, D) × S 个 query tokens
  ↓
[MultiSeqHyFormerBlock] × L 层（每层执行以下三步）：
    1. Seq Encoder（seq_encoder_type 变体） → 更新序列 KV
    2. CrossAttention（Query Decoding）   → query 从序列 KV 中提取信息
    3. RankMixerBlock（Query Boosting）   → [decoded_q ‖ ns_tokens] token mixing + FFN
  ↓
[Output Proj]               concat 所有 q tokens → (B, D)
  ↓
[Classifier]                Linear → (B, action_num) logits
```

### 4.2 NS Tokenizer

**RankMixerNSTokenizer**（默认）：
1. 对所有整型特征逐一查 Embedding（多值特征取均值池化）。
2. 全部 Embedding cat 成一条长向量 `(B, total_emb_dim)`。
3. 均匀切成 `num_ns_tokens` 段，每段过独立 Linear + LayerNorm 投影为 `(B, 1, D)`。
4. user/item 分开处理，分别得到 4 + 2 = 6 个 NS token；加 Dense 投影 token（1）+ 时间 NS token（1）= **共 8 个 NS token**。

**GroupNSTokenizer**（备选）：每组特征拼接后过一个 FFN 投影为 1 个 token，token 数 = 分组数。

**TimeNSTokenizer**（`use_time_ns=True`）：`hour_emb + dow_emb` → Linear + LayerNorm → 1 个独立时间 token。

**emb_skip_threshold**：vocab_size > 100 万的超高基数特征跳过 Embedding 分配，前向时以零向量代替（节省显存）。

### 4.3 序列编码器（seq_encoder_type）

| 类型 | 实现 | 复杂度 | 适用场景 |
|------|------|--------|---------|
| `swiglu` | SwiGLU FFN，无注意力 | O(L) | 极低时延 |
| `transformer`（默认） | Pre-LN 多头自注意力 + SwiGLU FFN | O(L²) | 高精度 |
| `longer` | 先 Top-K 压缩（最新 top_k 条）再自注意力 | O(L×top_k) | 工业在线 |

所有变体均支持可选 RoPE 位置编码（`--use_rope`）。

### 4.4 MultiSeqHyFormerBlock（每层执行）

```
for 每条序列 i:
    seq_tokens_i = seq_encoder_i(seq_tokens_i)     # 序列演化
    q_i = cross_attn_i(q_i, seq_tokens_i)           # Query Decoding（跨注意力）

combined = cat([q_0, q_1, ..., q_{S-1}, ns_tokens], dim=1)  # (B, T, D)
boosted  = RankMixerBlock(combined)                          # Query Boosting
→ 切分回各序列的 q_i 和 ns_tokens（残差更新）
```

**RankMixerBlock Token Mixing（参数免费）**：
```
(B, T, D) → reshape (B, T, T, d_sub) → transpose(1,2) → reshape (B, T, D)
→ per-token FFN → residual → post-LN
```
约束：`d_model % T == 0`，其中 `d_sub = d_model // T`。

### 4.5 参数统计与优化器分组

| 参数组 | 内容 | 优化器 | 说明 |
|--------|------|--------|------|
| 稀疏参数 | 所有 `nn.Embedding.weight` | Adagrad（lr=0.05） | 适合每步只有少量 id 被激活的稀疏梯度 |
| 稠密参数 | Linear、LayerNorm 等 | AdamW（lr=1e-4，β=(0.9,0.98)） | 标准 Transformer 训练配置 |

**高基数 Embedding 冷重启**（`reinit_sparse_after_epoch`）：从第 N 个 epoch 起，每轮结束对 vocab_size > `reinit_cardinality_threshold` 的 Embedding 做 Xavier 重初始化并重建 Adagrad 状态，防止高基数特征记忆训练集过拟合（参考 KuaiShou MultiEpoch）。

---

## 5. 训练循环（trainer.py）

### 5.1 主循环逻辑

```
for epoch in range(1, num_epochs+1):
    for step, batch in train_loader:
        loss = _train_step(batch)
        # 可选：每 eval_every_n_steps 步做一次 step 级评估
    
    # Epoch 级评估
    val_auc, val_logloss = evaluate()
    _handle_validation_result(...)   # EarlyStopping + 保存 best checkpoint
    
    # 高基数 Embedding 冷重启（epoch >= reinit_sparse_after_epoch）
    model.reinit_high_cardinality_params(reinit_cardinality_threshold)
```

### 5.2 训练步（_train_step）

```
1. 将 batch 搬到 GPU（non_blocking=True）
2. 构造 ModelInput NamedTuple
3. forward：model(input) → logits (B, 1)
4. 计算损失：BCE 或 Focal Loss（正负样本不均衡时使用）
5. backward
6. 梯度裁剪（max_norm=1.0）
7. dense_optimizer.step() + sparse_optimizer.step()
```

### 5.3 评估（evaluate）

```
1. model.eval()，torch.no_grad()
2. 遍历 valid_loader，收集所有 logits 和 labels
3. 过滤 NaN（梯度爆炸保护）
4. sklearn.roc_auc_score 计算 Binary AUC
5. F.binary_cross_entropy_with_logits 计算 LogLoss
```

### 5.4 损失函数

| 类型 | 适用场景 | 公式 |
|------|---------|------|
| BCE（默认） | 正负样本较均衡 | `-[y log p + (1-y) log(1-p)]` |
| Focal Loss | 极度不均衡（`--loss_type focal`） | `FL = -α_t (1-p_t)^γ log(p_t)` |

### 5.5 Early Stopping

- 监控指标：验证集 AUC（越高越好）。
- 连续 `patience`（默认 5）次评估无改善则触发停止。
- 每次新 best 自动保存 checkpoint 至 `global_step{N}.layer=L.head=H.hidden=D.best_model/model.pt`。
- checkpoint 目录自包含：同时复制 `schema.json`、`ns_groups.json`（若有）、`train_config.json`，推理时无需依赖训练机器原始路径。

### 5.6 TensorBoard 监控

| 曲线 | Tag | 写入时机 |
|------|-----|---------|
| 训练损失 | `Loss/train` | 每步 |
| 验证 AUC | `AUC/valid` | 每次评估（步级+epoch级） |
| 验证 LogLoss | `LogLoss/valid` | 每次评估 |

---

## 6. 关键超参速查

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `--d_model` | 64 | Transformer 主干维度 |
| `--emb_dim` | 64 | Embedding 维度 |
| `--num_queries` | 1（run.sh 中为 2） | 每条序列产生的 Global Query Token 数 |
| `--num_hyformer_blocks` | 2 | 堆叠 HyFormerBlock 层数 |
| `--num_heads` | 4 | 多头注意力头数 |
| `--seq_encoder_type` | transformer | 序列编码器变体 |
| `--hidden_mult` | 4 | FFN 内层维度倍数（相对 d_model） |
| `--dropout_rate` | 0.01 | 主干 dropout |
| `--batch_size` | 256 | 训练/验证 batch 大小 |
| `--lr` | 1e-4 | 稠密参数学习率（AdamW） |
| `--sparse_lr` | 0.05 | 稀疏参数学习率（Adagrad） |
| `--num_epochs` | 999 | 最大 epoch 数（实质由 early stopping 控制） |
| `--patience` | 5 | Early stopping 等待轮数 |
| `--seq_max_lens` | seq_a:256,seq_b:256,seq_c:512,seq_d:512 | 各域序列截断长度 |
| `--emb_skip_threshold` | 0（run.sh 中为 1000000） | 跳过 Embedding 的词表大小阈值 |
| `--ns_tokenizer_type` | rankmixer | NS Tokenizer 变体 |
| `--user_ns_tokens` | 0→自动 | rankmixer 模式 user NS token 数 |
| `--item_ns_tokens` | 0→自动 | rankmixer 模式 item NS token 数 |
| `--use_time_buckets` | True | 启用行为时间桶 Embedding |
| `--use_time_features` | True | 启用请求时刻连续时间特征（5维） |
| `--use_time_ns` | True | 启用离散时间 NS token |
| `--loss_type` | bce | 损失函数类型 |
| `--valid_ratio` | 0.1 | 验证集 Row Group 比例 |
| `--num_workers` | 16（run.sh 中为 8） | DataLoader worker 进程数 |

---

## 7. 代码文件职责一览

| 文件 | 职责 |
|------|------|
| `run.sh` | 启动脚本：设置 PYTHONPATH，传递激活配置 |
| `get_data_sample.py` | 从 HuggingFace 拉取演示 Parquet 样本 |
| `train.py` | 主入口：参数解析 → 数据加载 → NS分组 → 模型构建 → 训练 |
| `dataset.py` | Parquet IterableDataset：特征解析、Padding、时间特征、DataLoader |
| `model.py` | PCVRHyFormer 模型定义（所有 nn.Module） |
| `trainer.py` | 训练循环、评估、checkpoint、Early Stopping 协调 |
| `utils.py` | 通用工具：set_seed、EarlyStopping、LogFormatter、Focal Loss |
| `infer.py` | 推理入口（加载 checkpoint，输出 logit/embedding） |
| `ns_groups.json` | GroupNSTokenizer 的特征分组配置（备选模式） |
| `paper/HyFormer-notes.md` | HyFormer 论文阅读笔记（架构设计参考） |

---

## 8. 端到端数据流（前向传播）

```
Parquet Row
│
├─ user_int_feats (B, user_int_dim)  ──→  RankMixerNSTokenizer
│                                            Embedding(各fid) → cat → 均分 → Linear×4 → (B,4,D)
├─ item_int_feats (B, item_int_dim)  ──→  RankMixerNSTokenizer
│                                            Embedding(各fid) → cat → 均分 → Linear×2 → (B,2,D)
├─ user_dense_feats (B, D_dense)    ──→  Linear+LN → SiLU → (B,1,D)
├─ time_feats (B, 2)                ──→  hour_emb+dow_emb → Linear+LN → SiLU → (B,1,D)
│                                    ↓
│                         ns_tokens = cat [user_ns, user_dense, item_ns, time_tok]
│                                         → (B, 8, D)
│
├─ seq_a (B, S, L)  ──→  Embedding×S → cat → Linear+LN → GELU
├─ seq_a_time_bucket ─→  + time_embedding → (B, L, D)
│  ... (seq_b/c/d 同理) ...              → seq_tokens_list
│
└─ MultiSeqQueryGenerator(ns_tokens, seq_tokens_list)
     → q_tokens_list: [(B, Nq, D)] × 4 条序列

     ↓ MultiSeqHyFormerBlock × 2 层
     
     Block:
       seq_enc_i(seq_i) → new_seq_i         # 序列演化
       cross_attn(q_i, new_seq_i) → q_i'    # Query Decoding
     combined = cat(q_0',...,q_3', ns)      # (B, 16, D)
     boosted  = RankMixerBlock(combined)    # token mixing + FFN
     → 更新 q_tokens_list, ns_tokens
     
     output = concat(q_final) → Linear+LN  # (B, D)
     logits = Classifier(output)            # (B, 1)

Label (label_type==2) → BCEWithLogitsLoss → backward → AdamW + Adagrad
```

---

## 9. 常见问题与注意事项

### Token 数整除约束

`d_model % T == 0` 是 `RankMixerBlock` 在 `full` 模式下的硬约束。修改 `num_queries`、序列域数、NS token 数时必须重新验证。模型初始化时会主动检查并给出合法 T 值列表。

### 超高基数特征处理

- `emb_skip_threshold=1000000`：vocab > 100 万的特征在模型构建时不分配 Embedding，前向输出零向量，大幅节省 GPU 显存。
- 可以在不牺牲低基数特征精度的前提下过滤掉 item_id 等海量特征。

### 序列 id 特征额外 Dropout

vocab_size > `seq_id_threshold`（默认 10000）的序列特征被视为 id 类特征，训练时施加 `2×dropout_rate` 的额外 Dropout，降低对稀有 id 的记忆（过拟合）。

### Checkpoint 自包含设计

每个 `best_model` 目录同时包含 `model.pt`、`schema.json`、`ns_groups.json`（若有）、`train_config.json`，推理时只需指定 checkpoint 目录，无需访问原始训练数据目录。
