# 迭代 02 修复 —— 先修一条，再单独提交对照方向

> **Baseline（评测最优）**：Taiji 训练 **`ns time test`（内部 ID 91994）** — dense 时间特征 + 离散 **`use_time_ns`**；完整标识、曲线摘要与复现用 `run.sh` 见 **`hyper_log/baseline_ns_time_test.md`**。后续实验对比默认相对于该 baseline 陈述。

## 你现在要的流程（按顺序）

1. **只做一件事：修复迭代 02 类型失败**  
   提交 **`submit_bundle_iter02_fix`**（作业名当前为 **`taac_baseline_iter02_fix_v2`**）：入口 `run.sh` **必须先解压 `code.zip`** 再执行其中的 `train.py`，否则 Taiji 仍会跑模板里的旧 `train.py`，从而出现  
   `unrecognized arguments: --dense_weight_decay`。  
   此外，`run.sh` 使用 `set -u` 时，若集群环境未预设 `PYTHONPATH`，原来的 `export PYTHONPATH="${CODE_ROOT}:${PYTHONPATH}"` 会触发 **`PYTHONPATH: unbound variable`** 并立刻退出；已改为 **`export PYTHONPATH="${CODE_ROOT}:${PYTHONPATH:-}"`**。

2. **确认迭代 02 修复作业正常训练之后**，再提交 **对照深度**：**`submit_bundle_alt_depth`**（作业名当前为 **`taac_baseline_alt_depth3_v2`**，`batch_size=160` 以避免加深后的 CUDA OOM）。

不要求两条任务同时提交；第二步应在第一步验证无误后进行。（本条已于本地流程中完成提交，记录见下文「对照深度」。）

### 生成本地目录

```bash
cd ~/Desktop/taac_baseline   # 按你本机路径修改

bash hyper_log/make_code_zip.sh hyper_log/code_iteration04.zip
bash hyper_log/prepare_iter02_fix_bundle.sh
# 迭代02修复验证通过后，再执行：
bash hyper_log/prepare_alt_depth_bundle.sh
```

---

## 两条任务的迭代思路

两条 Taiji 作业建立在**同一套代码与数据管线**之上：RankMixer NS、`user_ns_tokens=4`、`item_ns_tokens=2`、`num_queries=2` 等与 baseline 一致；差异刻意收敛为**两类互不混杂的假设**，便于事后归因。

### 共同工程前提（否则迭代无从谈起）

1. **必须以 `code.zip` 解压后的 `train.py` 为准**  
   平台上模板自带的 `train.py` 往往落后于本地迭代；若直接执行模板副本，会出现 **`unrecognized arguments`**（例如 `--dense_weight_decay`）。因此两条任务的 `run.sh` 都在启动训练前解压 `code.zip`，并把 **`PYTHONPATH`** 指到解压目录（且对未定义变量使用 **`${PYTHONPATH:-}`**，兼容 `set -u`）。

2. **对照的可比性**  
   除「任务 B 单独写的结构/优化器相关超参」外，两条尽量共用 batch、worker、`patience`、`buffer_batches` 等，减少混杂变量。

---

### 任务一：`taac_baseline_iter02_fix_v2`（正则 / 优化主线）

| 维度 | 思路 |
|------|------|
| **要回答的问题** | 在**不改变默认 HyFormer 深度**（`num_hyformer_blocks` 默认 **2**）的前提下，通过 **Dense 侧权重衰减（AdamW `dense_weight_decay`）** 等方式抑制过拟合，能否稳住验证指标、改善迭代 02 附近出现的「拟合过快 / 泛化变差」类现象。 |
| **损失与优化** | 训练目标仍为 baseline 的 **BCE**；在 **dense 参数**上施加 **`weight_decay=0.01`**（由 `train.py`/`trainer.py` 中的 `--dense_weight_decay` 接入 AdamW），sparse 侧仍用独立 **`sparse_lr`**，与「只改结构」的路径区分开。 |
| **学习率** | **`lr=9e-5`**、**`sparse_lr=0.04`**，与加深网络那条相比略偏高一档：模型容量未放大时，允许稍强的 dense 更新步长；若验证仍抖动，再考虑与任务二交叉微调。 |
| **正则** | **`dropout_rate=0.05`**，作为与任务二（略低 dropout）对称的对照：任务一侧重「不加深网络时的正则强度」，而非靠减深度以外的结构改动。 |

**小结**：任务一是在 **固定容量（2 个 HyFormer block）** 下验证「**BCE + dense 权重衰减 + 既定 dropout/lr**」是否构成可靠的**抗过拟合主线**；工程上对应 `bundle_A_bce/run.sh` → `submit_bundle_iter02_fix`。

---

### 任务二：`taac_baseline_alt_depth3`（容量 / 结构主线）

| 维度 | 思路 |
|------|------|
| **要回答的问题** | 若 baseline 的瓶颈部分来自 **表征容量不足**（HyFormer 栈偏浅），把 **`num_hyformer_blocks` 从 2 增到 3** 能否在同等任务设定下带来更好的拟合—泛化折中；这需要与任务一**并行对照**，而不是在同一作业里同时改损失与深度。 |
| **结构** | **`--num_hyformer_blocks 3`**：加深交互栈，参数与路径长度变化；**不改损失形式**，仍为 BCE，且保留 **`dense_weight_decay=0.01`**，使两条任务在「是否有 dense 衰减」上可比（差别主要在深度与其它数值微调）。 |
| **学习率** | **`lr=8e-5`**、**`sparse_lr=0.038`**，相对任务一略降：更深网络对优化更敏感，用稍小的 dense/sparse 步长降低训练初期不稳定风险。 |
| **正则** | **`dropout_rate=0.045`**：略低于任务一；直觉上加深后模型更易欠正则，此处小幅下调 dropout 是为避免与「过强的 dropout + 更深网络」叠加导致欠拟合，但仍与任务一同量级，便于对照。 |
| **批次（工程约束）** | **`batch_size=160`**（低于默认 256）。加深一层 HyFormer 后激活 / 反向占用升高；Taiji 侧 GPU 常与其它进程共享显存，沿用 256 会在首个训练 step **`CUDA out of memory`**（作业 **106475**）。略减小 batch 以优先保证训练可跑通；与任务一对照时需注意 **batch 不等带来的梯度噪声差异**，结论以同设定下的曲线为主，数值勿机械点对点等同。 |

**小结**：任务二是在 **放大容量** 的前提下，用 **略保守的 lr 与 dropout** 做一次「**更深 HyFormer**」实验；工程上对应 `bundle_B_depth/run.sh` → `submit_bundle_alt_depth`。

---

### 如何对比两条结果

- **若任务一明显优于任务二**：可能当前数据与训练步数下 **2-block + 较强正则** 已足够，加深带来优化难度或冗余参数，优先考虑沿正则/lr 网格继续搜，而非继续加深。  
- **若任务二明显优于任务一**：提示 **容量** 可能是短板，可在「3 block + 任务二的 lr/dropout 邻域」内细调，或与任务一的 `dense_weight_decay` 做小规模组合试验（注意一次只改一类因素以利于归因）。  
- **若两者接近**：说明瓶颈可能在数据、特征或其它模块（而非单独深度或单独 wd）；下一轮迭代应回到日志与验证曲线，避免在同一窗口堆叠过多超参改动。

---

## 为什么有时会提示「要你本机跑 submit」？

- **`taac_iter02_overfit_mitigation`（106260）** 那次确实曾 **`--execute` 成功**：说明你当时的运行环境（网络能访问 Taiji API）允许 CLI 完成上传与创建作业。
- 之后在 Cursor **自动化沙箱**里执行同一命令时，多次出现 **`fetch failed`**：这是 **当前会话所在环境的出口网络/TLS 策略不稳定或被拦**，不是 Taiji 禁止脚本提交。
- 因此：**同一套命令在你本机终端通常可以自动提交**；在 IDE 内置环境里则可能失败。能自动提交时我会继续代跑；失败时才会请你本地执行同一命令。

---

## 提交命令（按需执行）

```bash
export PATH="$HOME/.local/bin:$PATH"
BASE="$HOME/Desktop/taac_baseline/hyper_log"
COOKIE="$HOME/Desktop/taac_baseline/hyper_log/taiji-cookie.txt"

# ① 仅迭代 02 修复（务必先做）
taac2026 submit \
  --bundle "$BASE/submit_bundle_iter02_fix" \
  --cookie-file "$COOKIE" \
  --template-job-internal-id 101210 \
  --allow-add-file \
  --execute --run --yes

# ② 修复跑通后，再提交对照方向（可选第二条）
taac2026 submit \
  --bundle "$BASE/submit_bundle_alt_depth" \
  --cookie-file "$COOKIE" \
  --template-job-internal-id 101210 \
  --allow-add-file \
  --execute --run --yes
```

---

## 记录：对照深度（容量方向）

### v2（`batch_size=160`，规避 CUDA OOM）

- **作业内部 ID**：**106531**
- **taskId**：`angel_training_ams_2026_1029735554728158280_20260514212339_3e425e87`
- **名称**：`taac_baseline_alt_depth3_v2`
- **本地结果**：`outputs/taiji-output/submit/live/2026-05-14T13-23-32-122Z/result.json`

### v1（失败：`CUDA out of memory`）

- **作业内部 ID**：**106475**
- **taskId**：`angel_training_ams_2026_1029735554728158280_20260514211347_3d090c7c`
- **名称**：`taac_baseline_alt_depth3`
- **现象**：`num_hyformer_blocks=3` 且 **`batch_size=256`** 时，首个 `_train_step` 在 `MultiSeqHyFormerBlock` / FFN 处 **`torch.OutOfMemoryError`**；日志显示单卡上尚有其它进程占用显存，与本训练进程叠加后无法再分配约 128 MiB。
- **本地结果**：`outputs/taiji-output/submit/live/2026-05-14T13-13-38-860Z/result.json`

---

## 记录：迭代 02 修复作业

### v2（PYTHONPATH 兼容 `set -u`）

一次成功的 **`taac2026 submit --execute --run --yes`** 结果：

- **作业内部 ID**：**106450**
- **taskId**：`angel_training_ams_2026_1029735554728158280_20260514210833_61529f28`
- **名称**：`taac_baseline_iter02_fix_v2`
- **本地结果**：`outputs/taiji-output/submit/live/2026-05-14T13-08-26-450Z/result.json`

### v1（已失败：未设置 PYTHONPATH）

- **作业内部 ID**：**106423**
- **taskId**：`angel_training_ams_2026_1029735554728158280_20260514210055_69a4e831`
- **名称**：`taac_baseline_iter02_fix`
- **现象**：Pod 日志出现 `run.sh: line 35: PYTHONPATH: unbound variable`（`set -u` + 引用未定义变量）。

本地为该次提交生成的 `result.json` 已随清理删除；以 Taiji 控制台记录为准。

---

## 迭代 03：基于 iter02_v2 / alt_depth3_v2 的效果与双任务

### 抓取到的验证指标（TensorBoard → `all-metrics-long.csv`）

- **Baseline（评测最优）`ns time test`（91994）**：验证 **AUC** 峰值约 **0.8688**（step ~38050），详见 **`hyper_log/baseline_ns_time_test.md`**。
- **`taac_baseline_iter02_fix_v2`（106450）**：`AUC/valid` 约在 step **52675** 达到峰值 **~0.868**，随后多轮验证下降至 **~0.864**（step **82775** 一带），表现为**中后期验证回落**，适合加强正则或略降学习率继续搜。

- **`taac_baseline_alt_depth3_v2`（106531）**  
  同期本地抓取中**未出现** `AUC/valid` 序列（可能未满首个 epoch、任务提前结束或 TB 未写入验证曲线）；**不能**与 iter02 直接对比验证强弱，迭代 03-B 仍保留 **3-block + 中等 batch** 方向，并与 03-A **正则对齐**以便对照。

### 迭代 03 两条训练（已提交）

| 分支 | Taiji 名称 | 内部 ID | taskId | 本地 `result.json` |
|------|------------|---------|--------|-------------------|
| **03-A** 正则加强（2-block） | `taac_baseline_iter03_reg` | **108422** | `angel_training_ams_2026_1029735554728158280_20260515091814_43b83ebb` | `outputs/taiji-output/submit/live/2026-05-15T01-18-10-968Z/result.json` |
| **03-B** 容量 + 对齐正则（3-block，`batch_size=176`） | `taac_baseline_iter03_depth3` | **108423** | `angel_training_ams_2026_1029735554728158280_20260515091820_5d965726` | `outputs/taiji-output/submit/live/2026-05-15T01-18-16-761Z/result.json` |

**超参相对 iter02_v2 / alt_depth3_v2**：两任务共用 **`dense_weight_decay=0.02`**、**`dropout_rate=0.06`**、**`lr=8e-5`**、**`sparse_lr=0.036`**、**`patience=12`**；03-A 为 **`num_hyformer_blocks=2`**、默认 batch **256**；03-B 为 **`num_hyformer_blocks=3`**、**`batch_size=176`**（OOM 时可将 `bundle_D_iter03_depth/run.sh` 改为 **160** 后重新 `prepare_iter03_depth_bundle.sh` 再提交）。

### 生成本地目录与提交命令

```bash
cd ~/Desktop/taac_baseline
bash hyper_log/make_code_zip.sh hyper_log/code_iteration04.zip
bash hyper_log/prepare_iter03_reg_bundle.sh
bash hyper_log/prepare_iter03_depth_bundle.sh

export PATH="$HOME/.local/bin:$PATH"
BASE="$HOME/Desktop/taac_baseline/hyper_log"
COOKIE="$HOME/Desktop/taac_baseline/hyper_log/taiji-cookie.txt"

taac2026 submit --bundle "$BASE/submit_bundle_iter03_reg" --cookie-file "$COOKIE" \
  --template-job-internal-id 101210 --allow-add-file --execute --run --yes

taac2026 submit --bundle "$BASE/submit_bundle_iter03_depth" --cookie-file "$COOKIE" \
  --template-job-internal-id 101210 --allow-add-file --execute --run --yes
```
